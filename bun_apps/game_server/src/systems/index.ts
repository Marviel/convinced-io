import { aiSystem } from './aiSystem';
import { interactionSystem } from './interactionSystem';
import { speechSystem } from './speechSystem';

export const systems = {
    aiSystem,
    interactionSystem,
    speechSystem,
};

export { aiSystem, interactionSystem, speechSystem };